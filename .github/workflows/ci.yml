name: Portfolio Tracker CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  audit-and-build-backend:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/workers/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: apps/api/package-lock.json

    - name: Install Dependencies
      run: |
        cd apps/api
        npm ci

    - name: Validate Prisma Schema
      run: |
        cd apps/api
        npx prisma validate

    - name: Build TypeScript Application
      run: |
        cd apps/api
        npm run build